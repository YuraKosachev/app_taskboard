CREATE TABLE IF NOT EXISTS users (id varchar(36) PRIMARY KEY,name VARCHAR(100) NOT NULL, hash VARCHAR(128) NOT NULL, email VARCHAR(100) UNIQUE NOT NULL, createdAt TIMESTAMP DEFAULT CURRENT_TIMESTAMP);